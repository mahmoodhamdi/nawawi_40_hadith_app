<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Default launch theme. This file was missing entirely: LaunchTheme only
         existed under values-night and values-v31, so a device on API 24–30 in
         light mode had no matching resource at all. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Applied as soon as the process has started; determines the window
         colour behind the Flutter UI while it initialises. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
